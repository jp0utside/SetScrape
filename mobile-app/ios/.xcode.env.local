# Local Xcode environment configuration
# This file is not versioned and contains local-specific settings

# Ensure we use the correct Node.js binary
export NODE_BINARY=$(command -v node)

# Set React Native environment variables
export RCT_METRO_PORT=8081
